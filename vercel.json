{
    "framework": "angular",
    "buildCommand": "npm run build",
    "outputDirectory": "dist/ai-resume-analyzer",
    "installCommand": "npm install",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}